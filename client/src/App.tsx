import { useState } from 'react';

function App() {
  const [count, setCount] = useState(0);

  return (
    <div className='App'>
      <div>here i promise to return</div>
    </div>
  );
}

export default App;
